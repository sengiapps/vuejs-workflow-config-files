const path = require('path');

const resolve = {
  alias: {
    /* Resolve directories */
    // Directory: path.resolve(__dirname, 'src/Directory'),

    /* Resolve Files */
    // file$: path.resolve(
    //   __dirname,
    //   'src/path/to/file.ext'
    // ),
  }
};

module.exports = resolve;
